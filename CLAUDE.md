@AGENTS.md

<!-- A regular file importing AGENTS.md, not a symlink to it: `omarchy plugin add`
     clones this repo as the plugin folder, and the Omarchy validator refuses any
     symlink inside one. -->
