---
status: draft
issue: 41
spec: spec/2026-09-24-41-cleanup.md
---

# Plan: The review's last findings, still in the code

## The approved decisions, carried over

**Delete two symbols:** `Model.js:1212` `containerById` and
`ResourceList.qml:44` `signal activated`. Both grep to their own declaration
and nothing else.

**Comment `rowsFor`, do not delete it.** Fifteen call sites for no runtime
gain; the defect was that a reader assumes production uses it.

**`labelMap` gets fixtures from observed output**, not assumption:
`podman images --format '{{json .Labels}}'` emits a real object here, `null`
for an unlabelled image, and `network ls` emits `""`.

**Four assertions for `copyTooltip`**, catching a reordered `if`.
**Three for `itemKey`**, the third being `rowId: ""` → `""` not `id`, which a
"simplification" to `item.rowId || item.id` would break.

**No behaviour change.** If a test fails when written, report it — do not
adjust the test around it. `labelMap` on `null` is the likely candidate.

**`copyValue` is out of scope** despite sitting beside `copyTooltip`.

## Steps

One commit per step, citing the step number and `#41`.

1. **Write the tests first, against unfixed code.** A new
   `tests/model/helpers.test.js` covering `itemKey` (3), `copyTooltip` (4) and
   `labelMap` (object from real output, `null`, `""`, comma string, and an
   object carrying a numeric `length` to pin that the branch is about shape).
   → verify by `node tests/run.js` green — these pin existing behaviour, so
   they should pass immediately. **Any failure is a finding to report, not to
   fix by editing the test.**

2. **Prove each new test discriminates**, in a scratch clone: change `itemKey`
   to `item.rowId || item.id` and the empty-string assertion must fail; swap
   two `copyTooltip` branches and its assertion must fail; make `labelMap`
   test truthiness instead of `length === undefined` and the shape assertion
   must fail.
   → verify by three failures and three recoveries, recorded in the commit.

3. **Delete `containerById`** from `Model.js`.
   → verify by `grep -rn '\bcontainerById\b' Model.js *.qml tests/` returning
   nothing, and the suite still green.

4. **Delete `signal activated`** from `ResourceList.qml`.
   → verify by `grep -rn '\bactivated\b' *.qml` returning nothing, and
   `nix flake check` green.

5. **Comment `rowsFor`** with the test-only note.
   → verify by reading it back.

## Tests

```bash
node tests/run.js    # expect green; 170 + the new test() blocks, counted not assumed
nix flake check      # expect: all checks passed, including the #24 and #30 checks
```

Plus `git diff master -- Panel.qml` empty.

No live desktop verification: nothing here changes runtime behaviour, no
capture is affected, and the QML change removes an unemitted signal.

## Rollback

Each step reverts alone. Steps 3 and 4 restore a symbol nothing referenced;
step 5 is a comment; steps 1 and 2 are additive tests. Reverting the tests
without the deletions, or the reverse, leaves a consistent tree either way.
